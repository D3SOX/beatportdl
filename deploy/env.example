# BeatportDL WebUI Docker Environment Variables
# Copy this file to .env and fill in your values

# HTTP Basic Auth credentials (required)
BASIC_AUTH_USER=admin
BASIC_AUTH_PASSWORD=changeme

# Port for the internal Astro server (optional, default: 3000)
# PORT=3000

# Override binary path if needed (optional)
# BEATPORTDL_BINARY_PATH=/app/bin/beatportdl-linux-arm64

